## aeroflare settings

Configure local preferences (theme, logins, cache URL)

### Synopsis

Configure local, per-machine preferences -- appearance theme,
registry logins, and the default cache URL -- and save them to aeroflare.yaml.

These settings are local to this machine. To configure the remote cache itself
(such as its signing public key), use "aeroflare configure".

```
aeroflare settings [flags]
```

### Options

```
  -h, --help   help for settings
```

### Options inherited from parent commands

```
      --cache-url string       OCI registry URL for the cache
      --cf-account-id string   Cloudflare Account ID
      --cf-token string        Cloudflare API Token
      --github-token string    GitHub Token
      --gitlab-token string    GitLab Token
  -v, --verbose count          Enable verbose output (-v for packages, -vv for requests)
```

### SEE ALSO

* [aeroflare](aeroflare.md)	 - A high-performance OCI-backed Nix binary cache proxy and toolkit

###### Auto generated by spf13/cobra on 19-Jul-2026
